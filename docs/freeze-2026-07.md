# 阶段性封存总结（2026-07）

> 状态：封存记录（快照，各线状态以本文日期为准）
> 日期：2026-07-07
> 基线：`QC/Wittgenstein` @ `a963ce4f9`
> 失效条件：任一被封存工作线重启开发时，须在本文对应小节标注"已重启：见 xxx"并链接新计划文档。
> 关联：`dev-plan-2026-07.md`（行动清单，§5 说明本文与它的关系）、`approval-mechanism-design.md`

## 1. 决策（2026-07-07）

- **所有已做的设计与实现原样保留**：代码不删、文档不改。本文只做索引与状态快照，不改写任何原文档。
- **工作流引擎 / 专家系统 / 可视化编辑器整条线封存**：不再推进下一阶段开发（Phase 3b、并行子专家、编辑器还债 P1/P2、编辑器归属决策均冻结）。需要时凭 §2 的入口重启。
- **权限审批 L1 保留并继续使用，进入试运行观察期**：跑一段时间再决定后续（L2 无人值守、删除回收暂存、per-session 作用域）。
- **明确不自研**：Cocos Creator MCP——社区已有实现，直接用，不做（推翻 dev-plan §4.1 的自研预期）。
- **暂缓**：高频操作固化为 skill（dev-plan §4.2）——尚未总结出高频操作清单，等清单自然浮现再做。
- **下一个开发目标评估中**：候选为引擎资产上下文（dev-plan §4.3，uuid ↔ 资源路径解析 + 资产引用关系）。是否立项另行记录。

## 2. 封存工作线状态

### 2.1 专家系统 + 工作流引擎接线

- **完成到哪**：Phase 1 软步骤（`ad6cca9b1`）、Phase 2 硬 delegate（`0ae5e60ff`）、Phase 3a 只读工具（`10d68e813`）、Phase 3c 副作用工具/技能 + checkpoint 回滚（`d9c1c34a4`）均已实现并有测试。宿主驱动 start/advance 模型、workflow registry、per-session 状态持久化、模式选择器 badge 齐备。
- **未做**：Phase 3b（MCP 工具硬执行）、并行子专家。
- **文档**：`expert-system-design.md`（总设计）、`workflow-demo-phase1.md`、`workflow-phase2-plan.md`、`workflow-phase3-plan.md`。
- **跨仓依赖**：引擎本体在 AIWorkflow 仓库（`/Volumes/workspace/GitHubTest/AIWorkflow`），对接契约见 `workflow-engine-handoff.md` §4；重启时须先核对契约双方是否漂移。

### 2.2 工作流可视化 + 编辑器

- **完成到哪**：MVP 已实现（`c5950da43`，React Flow 双文件架构）；P0 保存丢数据 / 加载合并偏差已修（`a6356c139`）。
- **冻结项**：`dev-plan-2026-07.md` §1 的 P1/P2 还债项、§2 编辑器归属决策（两仓各一份编辑器的局面维持现状）。
- **文档**：`workflow-dual-file-design.md`（设计）、`workflow-viz-editor.md`（实现记录 + 问题清单）。

### 2.3 APK 逆向工作流（标杆用例）

- **完成到哪**：v3（引擎分流 + env-check/env-gate + Godot 源检查 + demo 原型 + 配置指南，`7e4cd75f5` → `c26ec9c4c`）。
- **状态**：随工作流线一起封存；工作流文件 `.roo/workflows/apk-reverse-engineering.json` 保留可用。
- **文档**：`apk-reverse-workflow-design.md`。

## 3. 使用中 / 观察期

### 3.1 权限审批（会话级沙盒自治 L1）——观察期

- **完成到哪**：L1 端到端可用——`checkAutoApproval` 零影响叠加层 + 23 测试（`ef6b4961e`）、聊天工具栏盾牌开关（`f3b1fbe69`）、state 修复（`a963ce4f9`）。
- **回退点**：tag `开始新的权限审核机制`（@ `a6356c139`）。
- **观察期结论待定项**：L2 无人值守、删除回收暂存、per-session 作用域（见 `approval-mechanism-design.md` §7"明确不做"）。
- **文档**：`approval-mechanism-design.md`。

### 3.2 常规在用功能（不受本次封存影响）

以下均为已完成、日常在用的功能，列出仅为完整性：

- web_search / web_fetch 工具（Tavily，`894e24f41`）+ web-researcher 模式
- 记忆系统（`.roo/memory`）+ 自评 /reflect /remember（`de82f89ba` → `50f1c472f`）
- 内存占用优化 phase 1/2-A/2-B/2-C/3（`06005ba0e` → `d9adada86`；`memory-optimization.md` 状态"进行中"指后续阶段，已落地部分正常生效）
- Agent Loop 调试器 phase 1-4（`54782bb25` → `e28504604`；phase 5 打磨已明确延后，见 `2244dd5ee`）
- 增强提示词语言保持（`71ff67f61`）、技能/斜杠命令基础设施、QCode 品牌化

## 4. 重启入口清单

重启任一项时，从这里找到当时的断点：

| 事项                                     | 断点位置                                                              |
| ---------------------------------------- | --------------------------------------------------------------------- |
| Phase 3b MCP 硬执行                      | `workflow-phase3-plan.md`（3a/3c 实现记录）+ `dev-plan-2026-07.md` §3 |
| 并行子专家                               | `expert-system-design.md` + `dev-plan-2026-07.md` §3                  |
| 编辑器还债 P1/P2、归属决策               | `workflow-viz-editor.md` §5/§6/§8 + `dev-plan-2026-07.md` §1/§2       |
| L2 无人值守 / 删除回收暂存 / per-session | `approval-mechanism-design.md` §3/§7                                  |
| 调试器 phase 5                           | `debug-mode-design.md` 底部进度表                                     |
| 高频操作 skill                           | `dev-plan-2026-07.md` §4.2（方法论仍有效，等清单）                    |

## 5. 与 dev-plan-2026-07.md 的关系

按用户决定，`dev-plan-2026-07.md` 原文不改。以本文为准的覆盖关系：

- dev-plan §1（还债"先于一切新功能"）、§2（归属决策）、§3（短期主线）自本决策起**冻结**，不再执行。
- dev-plan §4 的方向分析仍有效，但：§4.1 引擎验证闭环中 Cocos MCP 改为**用社区实现**；§4.2 暂缓；§4.3 为当前候选目标；§4.4（3b 对准引擎 MCP）随工作流线冻结。
- dev-plan 自身失效条件③（2026-08 底前无更新则过期）继续生效——届时若重启规划，以新文档承接，不复活旧清单。

## 变更记录

- 2026-07-07 创建（基线 `a963ce4f9`）：记录封存决策；权限 L1 转观察期；下一目标候选 §4.3 评估中。
